{
  "name": "tripmate-ai",
  "version": "1.0.0",
  "description": "A thoughtful AI travel planner for better journeys.",
  "scripts": {
    "dev": "uvicorn app:app --host 0.0.0.0 --port 3000",
    "lint": "python3 -m py_compile app.py backend.py",
    "build": "python3 -m py_compile app.py backend.py",
    "start": "uvicorn app:app --host 0.0.0.0 --port 3000"
  },
  "dependencies": {},
  "devDependencies": {}
}
